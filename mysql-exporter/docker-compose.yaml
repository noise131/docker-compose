---
version: '3.8'
services:
  mysql-exporter:
    image: prom/mysqld-exporter:v0.14.0
    restart: always
    environment:
      DATA_SOURCE_NAME: monitor:monitor.123@(127.0.0.1:3306)/mysql
    network_mode: host
    # ports:
    #   - 9104:9104
...
